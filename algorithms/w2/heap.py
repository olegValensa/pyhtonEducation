'''
?????? ?? ????????????????: ??????? ? ????????????
?????? ?????? ????? ???????? ????? ???????? 1?n?105. ?????? ?? ??????????? n ????? ?????? ???????? ?????? ?? ????????? ???? ?????:
Insert x, ??? 0?x?109 — ????? ?????;
ExtractMax.
?????? ???????? ????????? ????? x ? ??????? ? ????????????, ?????? — ????????? ???????????? ????? ? ??????? ???.
https://stepic.org/lesson/%D0%9E%D1%87%D0%B5%D1%80%D0%B5%D0%B4%D0%B8-%D1%81-%D0%BF%D1%80%D0%B8%D0%BE%D1%80%D0%B8%D1%82%D0%B5%D1%82%D0%B0%D0%BC%D0%B8-13240/step/8?unit=3426
'''

def swap(i,j):
    temp = heap[i]
    heap[i] = heap[j]
    heap[j] = temp

def siftUp(i):
    while heap[i] > heap[i//2 + i%2 - 1] and i > 0: # i//2 + i%2 - 1  (i-1)//2
        swap(i,i//2 + i%2 - 1)
        i = i//2 + i%2 - 1

def insert(x):
    len_pre = len(heap)
    heap.append(x)
    siftUp(len_pre)

def extract():
    result = heap[0]
    heap[0] = -1
    delete(0)
    return result

def delete(i):
    if len(heap) > i*2+3:
        if heap[i*2+1]>heap[i*2+2]:
            swap(i,i*2+1)
            delete(i*2+1)
        else:
            swap(i,i*2+2)
            delete(i*2+2)
    elif len(heap) == i*2+3:
        if heap[i*2+1]>heap[i*2+2]:
            swap(i,i*2+1)
            del heap[i*2+1]
        else:
            swap(i,i*2+2)
            del heap[i*2+2]
    elif len(heap) == i*2+2:
        swap(i,i*2+1)
        del heap[i*2+1]

commands = []#['Insert 200', 'Insert 10', 'ExtractMax', 'Insert 500', 'Insert 50', 'Insert 20','ExtractMax']

n = int(input())

heap = []
for row in range(n):
    command = input().split()
    if command[0] == 'Insert':
        if (len(heap)) < 1:
            heap.append(int(command[1]))
        else:
            insert(int(command[1]))
    elif command[0].lower() == 'extractmax':
        print(extract())
    #print(heap)